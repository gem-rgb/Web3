# Scripts

- `dev-up.ps1`: starts the local stack with Docker Compose.
- `dev-down.ps1`: tears down the local stack and volumes.
- `generate-proto.ps1`: placeholder proto generation workflow for the canonical `proto/` tree.
- `lint.ps1`: runs the repository checks used by CI.
- `test.ps1`: runs tests across the workspace modules.

